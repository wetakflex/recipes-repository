FLEX RECIPES REPOSITORY
=======================

Para generar ref ejecutar:

symfony php -r 'echo bin2hex(random_bytes(20));'

Para reinstalar recipes ejecutar:

symfony composer recipes:install taklearning/alejandria-bundle --force -v